export function deploy2bunny(bunnyToken: string, distDir: string) {
  // make the http request to deploy bunny net
}

export function uploadScreenshot(bunnyToken: string, imagePath: string) {
  // make the http request to deploy bunny net
}
